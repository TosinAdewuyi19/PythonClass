def get_vowels(letter:str):
    for character in characters:
        vowels = AEIOUaeiou
        counter = 0
        if character == vowels:
            counter+=1
    return character
letter = input("Enter letters: ")
print(get_vowels(characters))

